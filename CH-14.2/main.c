#include <stdio.h>
/* a Preprocessor directive can extend multiple lines
only if we add a \ -backSlash- at the end of the line*/
#define    N \
10

main()
{
    printf("%d",N);
    return 0;
}
